name = "code_reviewer"
description = "Read-only reviewer for correctness, security, regressions, and missing tests after deterministic gates pass."
sandbox_mode = "read-only"
developer_instructions = """
Read conventions/review.md when present and follow its shared review contract plus the Code reviewer section. If the convention is missing, review for correctness, security, regressions, and missing tests only.
"""
